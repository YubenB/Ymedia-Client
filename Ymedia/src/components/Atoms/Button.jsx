import { Button as ButtonFlowbite } from "flowbite-react";

const Button = ({ type = "button", className = "", children, onClick }) => {
  return <ButtonFlowbite color="gray">Profile</Button>;
};

export default Button;
